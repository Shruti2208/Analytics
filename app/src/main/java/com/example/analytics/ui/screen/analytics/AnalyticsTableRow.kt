package com.example.analytics.ui.screen.analytics

// Replaces the old ItemsViewModel data class (renamed to avoid confusion with Android ViewModel)
data class AnalyticsTableRow(
    val title: String?,
    val value: String
)
